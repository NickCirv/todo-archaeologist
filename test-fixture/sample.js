// Sample file for testing todo-archaeologist

function doSomething() {
  // TODO: refactor this function
  const x = 1 + 1;
  
  // FIXME: handle null case here
  return x;
}

function legacyAuth() {
  // HACK: bypass auth check for now
  // WORKAROUND: temp fix until new API ships
  return true;
}

// XXX: this entire module needs a rewrite
// TEMP: remove before v2.0
